spring:
  servlet:
    multipart:
      max-file-size: 1000MB
      max-request-size: 1000MB
  datasource:
    url: jdbc:mysql://localhost:3306/school_rag?useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  ai:
    ollama:
      base-url: http://localhost:11434

      chat:
        model: llama3.2
        options:
          temperature: 0.7

      embedding:
        model: nomic-embed-text

logging:
  level:
    org.springframework.ai: INFO